{
  "name": "shorts-blocker-kids-website",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node scripts/check.mjs",
    "build": "node scripts/build.mjs",
    "smoke": "npm run build --silent && node scripts/smoke-test.mjs"
  }
}
